@model Sistema.Models.EsqueceuSenha

@{
    ViewBag.Title = "Recuperar senha E-mail de Recuperação";
}

<h1 class="heading-page">Solicitar recuperação de senha</h1>
<p class="sub-heading">Entre com seu E-mail de Recuperação e enviaremos uma solicitação de recuperação! ;)</p>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form single-col">

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.Label("E-mail de Recuperação", htmlAttributes: new { @class = "control-label col-md-2" })

            @Html.EditorFor(model => model.Email, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
        </div>

        <div class="form-btns">
            @Html.ActionLink("Voltar", "EsqueceuSenha", "Usuario", new { @class = "btn btn-secondary" })
            <input type="submit" value="Continuar" class="btn btn-primary" />
        </div>
    </div>
}